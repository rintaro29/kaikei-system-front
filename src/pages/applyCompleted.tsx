import React from "react";

const applyCompleted = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <p className="text-3xl font-bold">申請が完了しました</p>
    </div>
  );
};

export default applyCompleted;
